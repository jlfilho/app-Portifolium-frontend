import { RemoveRolePipe } from './remove-role.pipe';

describe('RemoveRolePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveRolePipe();
    expect(pipe).toBeTruthy();
  });
});
